services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: writekit
      POSTGRES_PASSWORD: writekit
      POSTGRES_DB: writekit
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
